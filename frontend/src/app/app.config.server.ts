import { ApplicationConfig } from '@angular/core';
import { appConfig } from './app.config';

// Server config not needed - using browser config only
export const config: ApplicationConfig = appConfig;

